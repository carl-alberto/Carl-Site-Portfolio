/**
 * setup an instance of the block
 *
 * @param {HTMLElement} element DOM Node of the block
 * @returns {void}
 */
function setupPortBlockBlock(element) {}

const allPortBlockBlocks = document.querySelectorAll('.wp-block-tenup-port-block');
allPortBlockBlocks.forEach(setupPortBlockBlock);
