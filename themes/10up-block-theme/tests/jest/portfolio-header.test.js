/* eslint-disable */
describe('Portfolio Header Block', () => {
	beforeEach(() => {
		jest.clearAllMocks();
	});

	it('test suite loads successfully', () => {
		expect(true).toBe(true);
	});

	it('block registration is ready', () => {
		// 10up-toolkit auto-handles @wordpress/blocks
		expect(true).toBe(true);
	});

	it('jest environment works', () => {
		expect(1 + 1).toBe(2);
	});
});
/* eslint-enable */
